import * as React from "react";
import contact from "assets/contact.png";

const contactStyle = {
  width: 40,
  height: 40
};

export const Contact = () => <img src={contact} alt="" style={contactStyle} />;
